We are in February 2025. You still only write code in a single file. Open `index.html` and append a second game below the first one: a "Guess My Number" game (1-100) using basic `while` loops and prompts. After that, add a "Cookie Clicker" game using a simple button and `localStorage` to keep score.

Break this implementation down into 30 granular micro-commits using the next sequential dates from Bucket 1.

COMMIT TEXT STYLE: "added guess game", "fixed infinite loop alert", "added score button", "localstorage works now", "changed text color to red".

HUMAN ERROR PATTERNS:
- Accidentally create an infinite while loop that freezes the browser tab. Commit the code, then commit a fix changing the condition block.
- Forget to parse the string from `localStorage` into an integer, breaking the counter. Commit the broken state, then commit the `parseInt()` fix.

Execute these 30 commits sequentially, applying the backdated git environment variables for each modification block.